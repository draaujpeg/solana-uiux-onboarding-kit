# Claude Code (local settings)
.claude/

# Operating system
Thumbs.db
.DS_Store

# Temporary
*.tmp
scratchpad/

# Dependencies (in case code components are added)
node_modules/
dist/

# Secrets & credentials (never commit these)
.env
.env.*
*.local
*.pem
*.key
*.keystore
*.p12
id_rsa
id_rsa.pub

# Editor / IDE
.vscode/
.idea/
*.swp

# Local working drafts (not part of the published kit)
analise-onboarding-heuristica.md
sintese-onboarding-design-system.md

# Raw evaluation data (per-app scores, named products). Working material, not
# part of the published kit: the research chapters carry what is meant to be
# public, and the skill is deliberately anonymous.
heuristic_avaliation.xlsx
*.xlsx

# Local install of our own skill, for dogfooding. install.sh --project writes
# these; the source of truth is skills/.
.agents/
