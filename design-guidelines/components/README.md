# Components

Design tokens and UI components that implement the [guidelines](../).

## Contents

- `tokens/` — design tokens (colors, typography, spacing, radii, shadows)
- `ui/` — components (code and/or Figma reference)

<!-- TODO: define the token format (W3C Design Tokens JSON, CSS vars, etc.) and the component stack -->
